{
  "name": "streamr-marketplace",
  "license": "AGPL-3.0-only",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ streamr-dev/streamr-marketplace",
    "create-local": "graph create --node http://localhost:8020/ streamr-dev/streamr-marketplace",
    "remove-local": "graph remove --node http://localhost:8020/ streamr-dev/streamr-marketplace",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 streamr-dev/streamr-marketplace"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.19.0",
    "@graphprotocol/graph-ts": "0.19.0"
  }
}
